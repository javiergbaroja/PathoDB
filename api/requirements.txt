fastapi>=0.111
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
aiofiles>=23.0
# Optional — enables automatic slide registration (/registration/auto).
# Manual landmark registration works without these.
numpy>=1.24
opencv-python-headless>=4.8

# Used by summarize.py (Ollama client) — previously transitively installed.
httpx>=0.27

# Optional — conversational agent + report RAG. The API degrades gracefully
# (HTTP 503 / disabled RAG) if these are absent. vLLM itself is served out of
# process from the conda env (see docs/AGENT_SERVING.md), not pinned here.
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.2
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0
sentence-transformers>=3.0
pgvector>=0.3
psycopg[binary]>=3.2

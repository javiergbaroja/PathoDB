
# PathoDB custom settings
port = 15432
log_directory = '/storage/research/igmp_dp_workspace/garciabaroja_javier/PW_reports/database/pathodb/logs'
logging_collector = on
log_filename = 'postgresql-%Y-%m-%d.log'

# Disable timeouts — this is a single-user research database on a closed network.
# Long-running ETL imports should never be killed by the server.
statement_timeout = 0
idle_in_transaction_session_timeout = 0
lock_timeout = 0
tcp_keepalives_idle = 60
tcp_keepalives_interval = 10
tcp_keepalives_count = 10

# PathoDB custom settings
port = 15432
log_directory = '/storage/research/igmp_dp_workspace/garciabaroja_javier/PW_reports/database/pathodb/logs'
logging_collector = on
log_filename = 'postgresql-%Y-%m-%d.log'

# Disable timeouts — this is a single-user research database on a closed network.
# Long-running ETL imports should never be killed by the server.
statement_timeout = 0
idle_in_transaction_session_timeout = 0
lock_timeout = 0
tcp_keepalives_idle = 60
tcp_keepalives_interval = 10
tcp_keepalives_count = 10
